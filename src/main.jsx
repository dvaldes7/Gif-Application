import React from "react";
import ReactDOM from "react-dom/client";
import { GifApplication } from "./GifApplication";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GifApplication />
  </React.StrictMode>
);
